To execute the scripts from the windows folder, please execute them from the root folder.
To execute the scripts from the rasp folder, cd into it, then execute them.

This is due to the use of relative paths and should be changed in the future.

Requirements.txt is only guranteed to work for windows systems, for raspberry pi, please use the provided docker.